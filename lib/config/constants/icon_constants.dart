import 'package:flutter/material.dart' show Icon, Icons;

abstract class IconConstants {
  static const Icon person = Icon(Icons.person_rounded);
  static const Icon like = Icon(Icons.thumb_up_rounded);
  static const Icon dislike = Icon(Icons.thumb_down_rounded);
  static const Icon menu = Icon(Icons.more_vert_rounded);
  static const Icon send = Icon(Icons.send_rounded);
  static const Icon arrowDown = Icon(Icons.keyboard_arrow_down_rounded);
  static const Icon add = Icon(Icons.add_rounded);
  static const Icon close = Icon(Icons.close_rounded);
  static const Icon rule = Icon(Icons.gavel_rounded);
  static const Icon notification = Icon(Icons.notifications_rounded);
  static const Icon arrowRight = Icon(Icons.arrow_right_rounded);
}
